Check for unresolved flagged comments. Run the flagged command from sprint-config.yaml (`backlog_commands.flagged`): `.venv/bin/agile-backlog flagged`. Display results clearly showing each flagged comment's item ID, comment text, and creation date. If no flagged comments exist, report "No flagged comments — all clear." If there are flagged comments, suggest actions: (1) resolve the comment, (2) respond to it, or (3) add related work to the current sprint scope. Arguments: $ARGUMENTS